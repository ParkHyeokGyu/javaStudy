package com.sist.manager;

public class Student {
	// default 변수 => 접근지정어르를 따로 지정하지 않았을 시
	// 같은 패키지에서는 사용할 수 있지만 다른 패키지에서는 사용 불가
	// public => 모든 패키지에서 접근 가능
	public int hakbun;
	public String name;
	public int kor,eng,math;
}
